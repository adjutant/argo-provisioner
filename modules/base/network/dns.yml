- name: Ensure /etc/hosts is correct
  action: template src={{ ansibledir }}/modules/base/network/hosts dest=/etc/hosts owner=root group=root mode=0644

#- name: Setup DNS updater - Install dependencies
#  action: pacman name=python2-pip state=latest

#- name: Setup DNS updater - Install cli53
#  action: pip executable=pip2.7 name=cli53 state=latest

#- name: Setup DNS updater - Transfer dnsupdater script
#  action: copy src={{ ansibledir }}/config/common/base/scripts/dnsupdater53 dest=/opt/scripts/dnsupdater53 owner=root group=root mode=0755

#- name: Setup DNS updater - Set trigger for boot time
#  action: file src=/opt/scripts/dnsupdater53 dest=/etc/dhcp/dhclient-exit-hooks.d/dnsupdater53 state=link

#- name: Setup DNS updater - Update internal DNS
#  action: command /opt/scripts/dnsupdater53
#  when: "{{ hostname_status.changed }}"
