- name: Ensure directory /opt/local exists
  action: file state=directory owner=operator group=devops mode=0755 path=/opt/local

- name: Ensure directory /opt/scripts exists
  action: file state=directory owner=operator group=devops mode=0755 path=/opt/scripts

- name: Ensure directory /etc/local exists
  action: file state=directory owner=operator group=devops mode=0755 path=/etc/local

- name: Ensure directory /var/log/local exists
  action: file state=directory owner=operator group=devops mode=0770 path=/var/log/local

- name: Ensure directory /tmp/src exists
  action: file state=directory owner=operator group=devops mode=0755 path=/tmp/src

# - name: Ensure directory /srv/api exists
#   action: file state=directory owner=operator group=devops mode=0755 path=/srv/api

# - name: Ensure directory /mnt/database exists
#   action: file state=directory owner=operator group=devops mode=0755 path=/mnt/database
